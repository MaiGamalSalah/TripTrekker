spring:
  application:
    name: FlightHotel-Services
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8081/realms/tripTrekker
  datasource:
    url: ${DB_URL:jdbc:postgresql://caboose.proxy.rlwy.net:53813/railway}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:aNDzJHxKygFXWFBmLqwGUmbvPAyGiqqP}
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.xml
    enabled: true

server:
  port: 8083

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    fetch-registry: true
    register-with-eureka: true
  instance:
    prefer-ip-address: true